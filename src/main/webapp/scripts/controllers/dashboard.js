'use strict';

/**
 * @ngdoc function
 * @name refiereApp.controller:DashboardCtrl
 * @description
 * # DashboardCtrl
 * Controller of the refiereApp
 */
angular.module('refiereApp')
  .controller('DashboardCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
