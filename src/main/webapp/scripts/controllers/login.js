'use strict';

/**
 * @ngdoc function
 * @name refiereApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the refiereApp
 */
angular.module('refiereApp')
  .controller('LoginCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
