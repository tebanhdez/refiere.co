'use strict';

/**
 * @ngdoc function
 * @name refiereApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the refiereApp
 */
angular.module('refiereApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
