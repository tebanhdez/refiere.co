'use strict';

/**
 * @ngdoc function
 * @name refiereApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the refiereApp
 */
angular.module('refiereApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
